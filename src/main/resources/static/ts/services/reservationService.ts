class ReservationService {
    list(params: {}) {
        return new Promise(() => {
        })
    }

    reserve() {
        return new Promise(() => {
        })
    }

    cancel() {
        return new Promise(() => {
        })
    }
}

export default new ReservationService()
