<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mrs.infrastructure.datasource.todo.TodoMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO todo (title, details, finished)
        VALUES (#{title}, #{details}, #{finished})
    </insert>
    <select id="select" resultType="mrs.domain.model.todo.Todo">
        SELECT id, title, details, finished
        FROM todo
        WHERE id = #{id}
    </select>
    <select id="selectAllJoin" resultType="mrs.domain.model.todo.Todo">
        SELECT id, title, details, finished
        FROM todo
    </select>
    <update id="update" parameterType="mrs.domain.model.Todo">
        update todo
        <set>
            <if test="title != null">
                title = #{title,jdbcType=VARCHAR},
            </if>
            <if test="details != null">
                details = #{details,jdbcType=VARCHAR},
            </if>
            <if test="finished != null">
                finished = #{finished,jdbcType=BIT},
            </if>
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>
</mapper>
